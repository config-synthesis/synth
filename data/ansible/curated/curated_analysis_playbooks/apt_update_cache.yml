- ansible:
    executable: ansible.builtin.apt
    arguments:
      update_cache: true
  analyze: true
  reduce:
    - ansible:
        executable: ansible.builtin.apt
        arguments:
          update_cache: true
