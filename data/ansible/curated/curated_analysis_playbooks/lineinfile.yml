- ansible:
    executable: ansible.builtin.file
    arguments:
      path: filename
      state: touch
- ansible:
    executable: ansible.builtin.lineinfile
    arguments:
      path: filename
      line: content
  analyze: true
  level: 0
